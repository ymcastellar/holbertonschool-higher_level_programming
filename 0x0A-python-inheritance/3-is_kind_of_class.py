#!/usr/bin/python3
"""[3. Same class or inherit from]
"""


def is_kind_of_class(obj, a_class):
    """[True if the object is an instance of object, otherwise False ]
    """
    return isinstance(obj, a_class)